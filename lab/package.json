{
  "name": "ondevice-film-lab-server",
  "version": "1.4.26",
  "private": true,
  "description": "Self-hosted shared photo library for OnDevice Film Lab",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/auth.js && node --check public/settings.js && node --check public/lab-editor.js && node --check ../photo-formats.js && node --check ../photo-codecs.js && node --check ../codecs/decode-worker.mjs && node --check public/lab-shell.js && node --check public/lab-viewport.js && node --check ../theme.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "better-sqlite3": "^12.2.0",
    "exif-reader": "^2.0.2",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "sharp": "^0.34.3"
  }
}
